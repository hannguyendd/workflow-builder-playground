[project]
name = "workflow-builder-playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "json-logic-qubit>=0.9.1",
]
