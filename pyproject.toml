[project]
name = "workflow-builder-playground"
version = "0.1.0"
description = "A Python workflow builder for defining and executing workflows composed of connected nodes"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "json-logic-qubit>=0.9.1",
]
